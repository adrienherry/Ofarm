const initialState = {
  speciesList: [],
  loading: 'true',
};

const speciesReducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default speciesReducer;
